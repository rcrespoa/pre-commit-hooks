-   id: pip-compile-lock
    name: Compiling pip dependency graph
    description: Compile requirements.[in|txt] into a locked dependency tree requirements file.
    entry: pip-compile-lock
    language: python
    stages: [commit, push, manual]
-   id: run-pytest-cov
    name: Pytest & Coverage
    description: Run tests and coverage report
    entry: run-pytest-cov
    language: python
    stages: [commit, push, manual]